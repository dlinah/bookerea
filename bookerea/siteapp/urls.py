from django.conf.urls import url
from . import views
# app_name="main"
urlpatterns = [
    # url(r'^books/(?P<pk>[0-9]+)$', views.index, name="book"),
    # url(r'^books/add$', views.AddBookView.as_view(), name="addbook"),
    # url(r'^books/edit/(?P<pk>[0-9]+)$', views.EditBookView.as_view(), name="editbook")
]
